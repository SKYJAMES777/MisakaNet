{
  "task_id": "lesson-git-tls-handshake-failure",
  "title": "GitHub TLS 握手失败 — gnutls_handshake() 错误",
  "domain": "devops",
  "tags": [
    "git",
    "github",
    "TLS",
    "SSL",
    "network"
  ],
  "problem": "`git pull` 或 `git push` 时报：",
  "solution": "```bash\n# 1. 先重试一次（如果是瞬时问题）\ngit pull origin main\n\n# 2. 如果持续失败，配置代理\ngit config --global http.proxy http://127.0.0.1:7890\ngit config --global https.proxy http://127.0.0.1:7890\n\n# 3. 验证连接\ngit ls-remote origin HEAD\n# 正常返回 HEAD commit hash\n```",
  "source": "lessons/contrib/git-tls-handshake-failure.md",
  "test_cmd": ""
}