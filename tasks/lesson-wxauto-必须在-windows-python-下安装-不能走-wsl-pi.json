{
  "task_id": "lesson-wxauto-必须在-windows-python-下安装-不能走-wsl-pi",
  "title": "Wxauto 必须在 Windows Python 下安装 不能走 Wsl Pip",
  "domain": "general",
  "tags": [
    "contrib"
  ],
  "problem": "在 WSL2 中 `pip install wxauto` 报错 `No matching distribution found`。wxauto 是 Windows-only 库。",
  "solution": "必须在 **Windows PowerShell 或 CMD** 中安装，不是在 WSL 终端：\n```powershell\n# 先退出 WSL\nexit\n# 在 Windows PowerShell 中\npip install wxauto requests\n```\n如果 PyPI 版本找不到，从 GitHub 源码安装：\n```powershell\npip install git+https://github.com/cluic/wxauto.git\n```",
  "source": "lessons/contrib/wxauto-必须在-windows-python-下安装-不能走-wsl-pip.md",
  "test_cmd": ""
}