{
  "task_id": "lesson-cron-job-not-running",
  "title": "Cron 作业不执行 / 不生效排障",
  "domain": "devops",
  "tags": [
    "cron",
    "scheduler",
    "not-running",
    "debug"
  ],
  "problem": "`crontab -e` 设置好后，作业从未执行。输出没有、日志没有、进程没有。",
  "solution": "```bash\n# 1. 检查 cron 是否运行\nsudo systemctl status cron\n# 或\nps aux | grep cron\n\n# 2. 打印当前 crontab\ncrontab -l\n\n# 3. 写入测试作业（确认 cron 工作机制）\ncrontab -e\n# 加一行：\n* * * * * echo \"CRON_ALIVE: $(date)\" >> ~/cron_test.log 2>&1\n\n# 4. 查看日志（大部分发行版）\nsudo tail -f /var/log/syslog | grep CRON\n# 或\nsudo journalctl -u cron -f\n\n# 5. 常见修复：在 cron 中显式设置 PATH\n# 在 crontab 顶部添加：\nPATH=/usr/local/bin:/usr/bin:/bin\nSHELL=/bin/bash\nHOME=/home/yourname\n\n# 6. Python 脚本在 cron 中的完整写法\n*/5 * * * * cd /path/to/project && /usr/bin/python3 script.py >> /tmp/script.log 2>&1\n```",
  "source": "lessons/contrib/cron-job-not-running.md",
  "test_cmd": ""
}