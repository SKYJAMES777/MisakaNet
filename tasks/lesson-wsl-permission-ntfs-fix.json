{
  "task_id": "lesson-wsl-permission-ntfs-fix",
  "title": "Permission Denied / WSL NTFS 跨文件系统权限修复",
  "domain": "devops",
  "tags": [
    "permission",
    "wsl",
    "ntfs",
    "eacces",
    "filesystem"
  ],
  "problem": "操作 `/mnt/c/` 下的文件时报 `Permission denied` 或 `EACCES`；或 `git` 在 `/mnt/c/` 下报错。",
  "solution": "```bash\n# 1. 检查是否是 NTFS 跨文件系统问题\ndf -T /path/to/file  # 确认文件系统类型\n# 如果显示 9p 或 drvfs → 是 WSL 挂载点\n\n# 2. 把项目移到 Linux 文件系统\nmv /mnt/c/Users/yourname/project ~/project/\n\n# 3. 如果必须在 /mnt/c/ 下工作，用 WSL 的 umask\nsudo umount /mnt/c\nsudo mount -t drvfs C: /mnt/c -o metadata,uid=$(id -u),gid=$(id -g),umask=22\n\n# 4. 修复单个文件\nsudo chown -R $(whoami):$(whoami) ~/project  # 只在 Linux 文件系统有效\n```",
  "source": "lessons/contrib/wsl-permission-ntfs-fix.md",
  "test_cmd": ""
}