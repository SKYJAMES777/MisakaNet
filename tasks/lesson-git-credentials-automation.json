{
  "task_id": "lesson-git-credentials-automation",
  "title": "Git 凭证配置 — 自动化 push 免密码",
  "domain": "devops",
  "tags": [
    "git",
    "credentials",
    "auth",
    "github"
  ],
  "problem": "脚本自动执行 `git push` 时弹出用户名密码输入框，导致自动化流程卡住。",
  "solution": "```bash\n# 1. 用 credential store（明文存储，仅限个人设备）\ngit config --global credential.helper store\necho \"https://username:${GITHUB_TOKEN}@github.com\" >> ~/.git-credentials\n\n# 2. 或使用 git-credential-oauth（更安全）\ngit config --global credential.helper oauth\n\n# 3. 测试\ngit ls-remote https://github.com/your-org/your-repo.git\n# 应该成功返回 HEAD 引用，无需交互\n```",
  "source": "lessons/contrib/git-credentials-automation.md",
  "test_cmd": ""
}