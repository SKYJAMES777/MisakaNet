{
  "task_id": "lesson-shared-json-needs-atomic-write",
  "title": "共享JSON状态需要原子写入",
  "domain": "devops",
  "tags": [
    "json",
    "atomic",
    "race-condition",
    "runtime"
  ],
  "problem": "多个自动化job同时写共享的运行时状态文件（如 latest.json），plain overwrite 会暴露半写状态导致并发读者解析失败。",
  "solution": "写共享JSON时使用：临时文件 + 原子 rename\n```python\nimport os, json, tempfile\ndef write_json_atomic(path, data):\n    with tempfile.NamedTemporaryFile('w', delete=False, dir=os.path.dirname(path)) as f:\n        json.dump(data, f)\n        tmp = f.name\n    os.rename(tmp, path)\n```",
  "source": "lessons/contrib/shared-json-needs-atomic-write.md",
  "test_cmd": ""
}